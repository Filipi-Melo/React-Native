import { StyleSheet } from 'react-native';

/**`Contém os estilos utilizados.`*/
export default StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems:'center',
    backgroundColor: '#ffffff',
    padding: 8
  },
  paragraph: {
    margin: 10,
    fontSize: 18,
    fontWeight: 'bold',
    textAlign: 'center'
  }
});